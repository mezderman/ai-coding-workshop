# dependencies / build
node_modules/
.next/
*.tsbuildinfo

# local database (regenerated on first run)
sqlite.db
sqlite.db-*

# Claude Code local overrides
.claude/settings.local.json

# Matt Pocock skills — installed locally via `npx skills@latest add mattpocock/skills`
.agents/
.claude/skills/
skills-lock.json

# macOS
.DS_Store
